/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "mdbcdn.b-cdn.net",
      "random.imagecdn.app",
      "ichef.bbci.co.uk",
      "raw.githubusercontent.com",
    ],
  },
};

export default nextConfig;
